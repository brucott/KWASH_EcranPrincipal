if (#.simulAlarmes[0] = TRUE) 
	#.Alarmes[0] := TRUE;
else
	#.Alarmes[0] := FALSE;
endif

if (#.simulAlarmes[1] = TRUE) 
	#.Alarmes[1] := TRUE;
else
	#.Alarmes[1] := FALSE;
endif

if (#.simulAlarmes[2] = TRUE) 
	#.Alarmes[2] := TRUE;
else
	#.Alarmes[2] := FALSE;
endif
